class Reflector extends Tile {
    constructor(col, row) {
        super(col, row);
        this.color = {
            r: 34,
            g: 49,
            b: 63
        };
    }

    // Reflect the neutron
    // TODO
    onReact(n) {}
}
